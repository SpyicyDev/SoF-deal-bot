# Local secrets for `act` runs. Copy to `.secrets` and fill in.
# `.secrets` is gitignored — never commit the real values.

ANTHROPIC_API_KEY=sk-ant-...
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/T.../B.../...
GITHUB_TOKEN=ghp-...   # only needed if you want open_failure_issue.py to actually hit the API
